apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kustomize-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform-team
  source:
    repoURL: https://github.com/spelluri/argocd-apps
    targetRevision: HEAD
    path: manifests/kustomize-app/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: kustomize-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
