apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nodeapp-prod
namePrefix: prod-

commonLabels:
  environment: prod

resources:
  - ../../base

patches:
  - path: patch.yaml
