apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nodeapp-staging
namePrefix: staging-

commonLabels:
  environment: staging

resources:
  - ../../base

patches:
  - path: patch.yaml
